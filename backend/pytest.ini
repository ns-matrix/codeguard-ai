[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    llm: tests that call the real Ollama API (slow)
    integration: tests that require PostgreSQL
filterwarnings =
    ignore::DeprecationWarning
